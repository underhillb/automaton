<%
extra_opt='
filtering: false,
inserting: true,
editing: false,
removing: false,

fields: [
{ name: "id", type: "text", itemTemplate: function(value) {
                            var output= $("<a>").attr("href", "blueprints/"+value).text("View")
                            output.append("/")
                            output.append($("<a>").attr("href", "blueprints/"+value+"/edit").text("Edit"));
                            return output;
                        } , width: 200, filtering: false,width: 50,title: "View", editing: false
  },
{ name: "name", type: "text",title: "Blueprint" },
{ name: "description", type: "text",title: "Description" },
{ name: "blueprint_objects", width: 200, filtering: false, title: "Objects",
  itemTemplate: function(value) {
    if (value) {
  var $info=$("<div>").addClass("client-info")
  for (var i = 0; i < value.length; i++) {
    $info.append($("<p>").text(value[i].name));
  }
    for (var blueprint_object  in value) {
  }
  return $("<tr>").append($("<td>").append($info));
  }
}
  },
{ name: "version", type: "number",filtering: false, title: "Version",editing: false,inserting: false, },
{ name: "user", type: "text",filtering: false,title: "User",editing: false,inserting: false, },
{ type: "control",deleteButton: true,editButton: false }

]'
%>
<%= render 'partials/grid', :ajax_url => "blueprints",:extra_opt => extra_opt  %>
